# TODO(Milestone 5 - Prediction Engine): deterministic load forecasting
# (moving average / linear regression + thresholds) and overload risk estimation.
# This module owns the health-status and overload-risk decision; it must remain
# deterministic and explainable, and must not call the Claude API.
