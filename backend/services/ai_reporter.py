# TODO(Milestone 9 - AI Operator Summary): call the Claude API to turn a
# critical alert's telemetry/prediction data into a plain-English operator
# summary. Pure text generation only — must not alter health status, alerts,
# or prediction values.
